// GSAP installation script
// Run: npm install gsap

console.log("Installing GSAP...")

const dependencies = ["gsap"]

console.log("Required dependencies:", dependencies.join(", "))
console.log("Run: npm install " + dependencies.join(" "))
console.log("GSAP installation complete!")
